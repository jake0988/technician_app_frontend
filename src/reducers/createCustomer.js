// const initialState = [];

// export default (state = initialState, action) => {
//   switch (action.type) {
//     case "CREATE_NEW_CUSTOMER":
//       return state.concat(action.formData);
//     default:
//       return state;
//   }
// };
